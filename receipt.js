(() => { "use strict"; const root = document.querySelector("#receipt-content"); const money = value => `PKR ${new Intl.NumberFormat("en-PK").format(Number(value) || 0)}`; let order; try { order = JSON.parse(sessionStorage.getItem("kashmir-roll-point-order")); } catch {} if (!order || !Array.isArray(order.items) || !order.items.length) { root.innerHTML = `<div class="empty-state"><h1>Order not found</h1><p>Start a new order from the menu.</p></div>`; return; } const date = new Date(order.date); const items = order.items.map(item => `<tr><td>${escapeHtml(item.name)}</td><td>${item.quantity}</td><td>${money(item.price)}</td><td>${money(item.price * item.quantity)}</td></tr>`).join(""); root.innerHTML = `<div class="receipt-heading"><div><p class="eyebrow">Order received</p><h1>Thank you!</h1><p class="muted">Order ${escapeHtml(order.orderNumber)} · ${date.toLocaleString()}</p></div></div><section class="receipt-details"><h2>Customer details</h2><p><strong>Name:</strong> ${escapeHtml(order.customerName)}<br><strong>Phone:</strong> ${escapeHtml(order.customerPhone)}<br><strong>Address:</strong> ${escapeHtml(order.customerAddress)}<br><strong>Payment:</strong> Cash on Delivery</p></section><section><h2>Order details</h2><div class="receipt-table-wrap"><table><thead><tr><th>Item</th><th>Qty</th><th>Price</th><th>Subtotal</th></tr></thead><tbody>${items}</tbody></table></div><p class="receipt-total"><strong>Total</strong><strong>${money(order.total)}</strong></p></section>`; function escapeHtml(value) { const node = document.createElement("div"); node.textContent = String(value ?? ""); return node.innerHTML; } })();
